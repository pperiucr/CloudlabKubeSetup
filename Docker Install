# Setting up docker
### Download all the files from GIT repo
```bash
sudo mkdir /mydata
sudo cd /mydata && sudo git clone https://github.com/pperiucr/CloudlabKubeSetup.git
```

### Download and install docker
```bash
sudo chmod +x ./dockersetup.sh
sudo source ./dockersetup.sh
```
